#include <stdio.h>

int iIsPalindrome(int* a, int n) {
	for (int i = 0; i < n; i++) {
		if (a[i] != a[n-1-i]) {
			return 0;
		}
	}
	return 1;
}

int main() {
	int a[] = {1,2,2,1};
	printf("%d", iIsPalindrome(a,4);
	int b[] = {1,2,3,4};
	printf("%d", iIsPalindrome(b,4);
}